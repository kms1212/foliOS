{
    "timezone": 32400,
    "rtc_utc": true,
    "start_script": [
        "loadmodule boot:/modules/load_folios.mod",
        "mount fd0pt0p2 sys",
        "load_folios sys:/System/Kernel/strata.elf -ramdisk sys:/packages -sysman /SystemManager/SystemManager.app -log.enable=true -log.level=debug -log.mask.thread=lifecycle,task_switch -power.method=acpi"
    ]
}
